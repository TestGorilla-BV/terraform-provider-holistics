---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "holistics Provider"
subcategory: ""
description: |-
  Manage Holistics (https://holistics.io) resources via the v2 API.
---

# holistics Provider

Manage Holistics (https://holistics.io) resources via the v2 API.

## Example Usage

```terraform
terraform {
  required_providers {
    holistics = {
      source = "TestGorilla-BV/holistics"
    }
  }
}

variable "holistics_api_key" {
  type      = string
  sensitive = true
}

provider "holistics" {
  api_key = var.holistics_api_key
  region  = "apac"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `api_key` (String, Sensitive) Holistics API key (sent as X-Holistics-Key). May be set via the HOLISTICS_API_KEY environment variable.
- `base_url` (String) Override the API base URL (e.g. for testing). May be set via HOLISTICS_BASE_URL.
- `region` (String) Holistics region. One of `apac`, `us`, or `eu`. Defaults to `apac`. Ignored if `base_url` is set. May be set via HOLISTICS_REGION.
